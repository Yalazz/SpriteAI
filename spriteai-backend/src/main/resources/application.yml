server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/spriteai
    username: spriteai
    password: spriteai
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  redis:
    host: redis
    port: 6379

logging:
  level:
    root: INFO
